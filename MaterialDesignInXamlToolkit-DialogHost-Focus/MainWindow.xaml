<Window x:Class="MaterialDesignInXamlToolkit_DialogHost_Focus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MaterialDesignInXamlToolkit_DialogHost_Focus"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        Title="MainWindow" Height="450" Width="800">
    <materialDesign:DialogHost>
        <materialDesign:DialogHost.DialogContentTemplate>
            <DataTemplate>
                <DataTemplate.Resources>
                    <DataTemplate DataType="{x:Type local:ViewModel}">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.ColumnSpan="3" Margin="0,0,0,5">
                                <!--<TextBlock Text="Username" />-->
                                <TextBox Text="{Binding LastInput}" />
                                <!--<TextBlock Text="Password" />
                                <TextBox Text="{Binding Password}" />-->
                            </StackPanel>

                            <Button Content="_Login" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Grid.Row="1" Grid.Column="2">
                                <Button.CommandParameter>
                                    <system:Boolean>True</system:Boolean>
                                </Button.CommandParameter>
                            </Button>
                            <Button Content="_Cancel" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Grid.Row="1" Grid.Column="0" />
                        </Grid>
                    </DataTemplate>
                </DataTemplate.Resources>

                <ContentControl Content="{Binding}" />
            </DataTemplate>
            
        </materialDesign:DialogHost.DialogContentTemplate>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel>
                <TextBlock Margin="0,0,10,0"
                           Style="{StaticResource MaterialDesignTextBlock}">User input is:</TextBlock>
                <TextBlock Text="{Binding LastInput}"
                           Style="{StaticResource MaterialDesignTextBlock}"/>
            </DockPanel>

            <Button Grid.Row="1"
                    Click="Button_Click">Change user input</Button>
        </Grid>
    </materialDesign:DialogHost>
</Window>
